import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class Po_admin extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                Po Admin
            </div>
        )
    }
}
