import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class Report_sourcing_all extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                Report_sourcing_all
            </div>
        )
    }
}
